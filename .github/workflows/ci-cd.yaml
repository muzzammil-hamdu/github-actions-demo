name: CI-CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy to VM
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: 20.243.209.127
          username: mujju
          key: ${{ secrets.SSH_KEY }}
          script: |
            cd /home/mujju/app
            git pull
            echo "Deployed successfully 🚀"
